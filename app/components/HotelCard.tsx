import React from 'react';\n\ninterface HotelCardProps {\n    image: string;\n    name: string;\n    rating: number;\n    price: number;\n}\\n\nconst HotelCard: React.FC<HotelCardProps> = ({ image, name, rating, price }) => {\n    return (\n        <div className="hotel-card">\n            <img src={image} alt={name} className="hotel-image" />\n            <h2 className="hotel-name">{name}</h2>\n            <p className="hotel-rating">Rating: {rating} ⭐</p>\n            <p className="hotel-price">Price: ${price.toFixed(2)}</p>\n        </div>\n    );\n};\n\nexport default HotelCard;